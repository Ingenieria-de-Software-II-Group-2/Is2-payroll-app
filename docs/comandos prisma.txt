# instalar y init
npm install prisma --save-dev
npm install @prisma/client
npx prisma init

# (editar .env con DATABASE_URL de Neon)
# subir schema (dev rápido)
npx prisma db push

# o: crear migración (recomendado)
npx prisma migrate dev --name init

# aplicar migraciones en producción / CI
npx prisma migrate deploy

# generar client (si no se generó)
npx prisma generate
